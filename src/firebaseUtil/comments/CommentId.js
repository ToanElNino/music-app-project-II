import { db } from "../../../firebase";
import { set, ref, getDatabase } from 'firebase/database';

export async function GetCommentId(songId){
    console.log('call get comment id');
}
export async function UpdateCommentId(songId){
    console.log('call update comment id');
}